GENERATION gen-2: no clones. Work that milestone only; its description is the
scope bar. SETTLED, cite and do not re-open: #303 ruled the entry point TAKES
THE REPO AS AN ARGUMENT, shipped in the dose payload; #306 holds the invariant
and the acceptance test. BUILT, confirm and do not recreate: everything but
`schedule/` is already served -- `gh api repos/hf7y/verbs/contents/scheduler/bin`
lists it, and `.../scheduler/schedule` 404s, which is the whole gap.
`fetch_repo_file` in `lib/dose-common.sh` is the reader. PROHIBITED: monkey stays pinned to `2026-09-01T030800Z`, approving a
build is a human gate, and no clone is removed until #306 passes -- stop READING
from them first or you disarm the fleet. PROVED on vaporwave per #304, by a
dispatch reaching `claude -p` in an account holding no clone. Clone-free was
declared done in commit prose three times while fourteen of fifteen accounts held
one: land a witness or land nothing.

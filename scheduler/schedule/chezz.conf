# chezz -- the game. RE-REGISTERED 2026-08-04 to move it onto monkey.
#
# Zach, 2026-08-04: "move over one more service to monkey and get it set up.
# I think chezz would be good."
#
# WHY THERE WAS NO conf FILE TO EDIT. chezz used to be a participant and was
# deregistered: schedule/_paced.conf's own footer still records it --
# "Rows removed here: chezz, chezz-sweep, home-assistant, vkv-inventory, ..."
# -- and this file was deleted with them, while _paced.conf line 116 kept
# pointing at "schedule/chezz.conf" for an explanation that no longer existed.
# That dangling reference is repaired by this file existing again.
#
# WHERE THE PROJECT ACTUALLY IS, re-probed 2026-08-04 rather than assumed:
#   * hf7y/chezz on GitHub -- PUBLIC, default branch main, last pushed
#     2026-08-01. Substantial: index1.html, package.json, playwright config,
#     leaderboard/, tools/, .clasp.json (Google Apps Script), and its own
#     .scheduler/FOCUS.md + QUESTIONS.md. Scheduler-shaped already.
#   * /home/zach/ecosystem1/ecosystem1/chezz -- a CONSIGNED copy in the vault
#     (front-matter: consigned 2026-08-01), not a working tree.
#   * /home/zach/Documents/Project Archive/chezz -- the path the consign
#     header names as its source. GONE as of 2026-08-04.
# So the GitHub remote is the only canonical copy, which is what makes moving
# it to a fresh host a clone rather than a migration. Nothing has to be
# rescued off mandark first.
#
# PUBLIC REPO, unlike every other self-dev participant. Two consequences worth
# stating because they are easy to get wrong in opposite directions:
#   * the read path needs no credential at all;
#   * the WRITE path still does, and `bin/wire-selfdev-git.sh chezz --apply
#     --rw` issues a read-write deploy key exactly as for a private repo.
#     ecosim's own scheduler key is read-only on a public repo for the same
#     reason: the wiring rewrites every github.com URL onto a per-repo alias,
#     so "public" changes nothing about which key is presented.

PROJECT="chezz"
PROJECT_KEY="chezz"
# --- which account and host own this job's STATE ----------------------------
# CRON_ACCOUNT is what `scheduler`'s state_account()/state_home()/state_run()
# read to answer "whose ~/.local/share holds this job's run log" -- unset, the
# glance reads $HOME instead and reports a live job as idle/dead-man expired
# (hf7y/scheduler#33).
#
# CRON_HOST is load-bearing, not decorative: sync-crontab.sh checks it FIRST
# and `continue`s cleanly, because chezz is a real account only on monkey --
# without it, syncing from mandark errors instead of skipping.
# Host first, account second, in that order.
CRON_HOST="monkey"
CRON_ACCOUNT="chezz"


PROJECT_REPO_PATH="$HOME/Documents/Projects/chezz"

REPO_URL="https://github.com/hf7y/chezz.git"
REPO_SUBDIR="."

# ===========================================================================
# TIER 1 -- bug sweeper: BLANK, and not merely "not yet".
# ===========================================================================
# nothing dispatches a Tier 1 tick on monkey (or anywhere -- the
# sync-crontab.sh mechanism that ever installed one is retired, #495), so a
# job name here would be inert and would read as armed to anyone who did not
# know that. chezz DID have a Tier 1 sweep on mandark --
# ~/.local/share/chezz-bug-sweep still holds its state, and
# INCIDENT-2026-08-01-bug-sweep-oidc.md in the repo is about that sweep
# hitting an OIDC failure. Reviving it is a separate decision from moving the
# project, and it belongs to whoever reads that incident first.
SWEEP_JOB_NAME=""

# ===========================================================================
# TIER 2 -- overnight batch.
# ===========================================================================
BATCH_JOB_NAME="chezz-nightly-batch"

# THE VERDICT BLOCK IS NOT BOILERPLATE. bin/usage-paced-runner.sh classifies
# every finished run through verdict.sh: a run that records nothing is
# NOT-DONE and gets re-dispatched with metabolism untouched, and "CONTINUE
# (queue not empty)" is the wrong test when the only thing open is blocked on
# a human. Both lessons are folded into @@FRAGMENT:run-procedure@@ below.
#
# $HOME expands because schedule/*.conf is SOURCED, unlike _paced*.conf which is
# read with `while IFS='|' read` -- MONKEY.md section 4.
BATCH_PROMPT="@@FRAGMENT:run-procedure@@

chezz is the only account that has ever hit the turn ceiling: a 2026-08-05T14:24
tick ran 1786s against 306-708s elsewhere and was cut off mid-run
(hf7y/scheduler#31). The likely cause is now measured rather than guessed --
this repo's pre-commit hook runs the full 177-test Playwright suite, 4.1 min per
commit on mandark. Do not run \`npm run check\` more times than you need to, and
if you touch nothing the suite covers, say so rather than paying for it twice.

You share this repo with a GitHub Actions runner (.github/workflows/agent.yml,
daily 09:00 UTC) and nothing locks between you. Before starting an issue, check
that no open PR and no recent branch already covers it."

# 120 is deliberate, not the untouched default: a prior 2x probe window sat
# disabled and unspent, and re-arming at that value would adopt an untested
# probe as the baseline by accident.
BATCH_MAX_TURNS="120"


# Without ANSWER_CHANNEL, `issues_counts()` never consults the issues channel
# for a project that doesn't declare one -- chezz's own question issues sat
# answered-but-unseen for this reason (#3-#6). The fix and the wiring are one
# change on purpose: a predicate corrected in a repo the glance never queries
# is not verified where the consumer reads it.
ANSWER_CHANNEL="issues"

USES_STANDING_RULES=1

name: runtime

on:
  pull_request:
    branches: [bashified, main]
  push:
    branches: [bashified, main]

jobs:
  runtime:
    uses: hf7y/etalon/.github/workflows/guard.yml@main
    with:
      runtime: true
      prose: false
      runs_on: '["self-hosted","monkey"]'
